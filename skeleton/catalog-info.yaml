apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
  annotations:
    backstage.io/techdocs-ref: dir:.
    github.com/project-slug: ${{ values.repoUrl.split('/')[-2] }}/${{ values.repoUrl.split('/')[-1] }}
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner }}
